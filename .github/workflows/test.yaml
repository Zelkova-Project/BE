on:
  pull_request:
    branches: [ develop, release, master ]

jobs:
  load:
    uses: ./.github/workflows/load.yaml

  test:
    runs-on: ubuntu-latest
    needs: load

    steps:
      - name: 테스트
        env:
          BRANCH: ${{ needs.load.outputs.branch }}
        run: |
          if [ $BRANCH ]; then
            ./gradlew test -Pprofile=$BRANCH
          else
            ./gradlew test
          fi
